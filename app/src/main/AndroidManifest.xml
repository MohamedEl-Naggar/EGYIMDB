<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.aucegypt.egyimdb">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".EmailClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EGYIMDB">
        <activity android:name=".MovieReviews"></activity>
        <activity android:name=".CastRoles" />
        <activity android:name=".CastInfo" />
        <activity android:name=".MovieInfo" />
        <activity android:name=".SearchByGenre" />
        <activity android:name=".SearchCast" />
        <activity android:name=".SearchMovies" />
        <activity android:name=".Top10" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SignupActivity"
            android:label="@string/title_activity_signup" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>